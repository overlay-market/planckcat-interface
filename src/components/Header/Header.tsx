import React from "react";
import styled from "styled-components";

const Container = styled.div`
  display: block;
  width: 100%;
`

export const Header = () => {
  return (
    <Container>
      Header
    </Container>
  )
};