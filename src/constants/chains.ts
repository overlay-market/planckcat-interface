export enum SupportedChainId {
  MAINNET = 1, //at launch
  KOVAN = 42, //pre-launch only
  ROPSTEN = 3, //pre-launch only
  RINKEBY = 4, //pre-launch only
  LOCALHOST = 1337, //development
  LOCALHOSTDEV = 1641924983333 //local mm
};
